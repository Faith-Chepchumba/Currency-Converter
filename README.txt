# Currency Converter

## Overview

A simple command-line currency converter that fetches exchange rates from an API.

## Features

- Convert any currency to another.
- Real-time exchange rates.

## Technologies Used

- Python
- requests library

## How to Run

1. Install the requests library if not installed: `pip install requests`

2. Run the script: `python currency_converter.py`